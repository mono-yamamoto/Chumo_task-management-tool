name = "chumo-api"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "development"

[[r2_buckets]]
binding = "UPLOAD_BUCKET"
bucket_name = "chumo-uploads"

# [env.staging]
# vars = { ENVIRONMENT = "staging" }

# [env.production]
# vars = { ENVIRONMENT = "production" }
